import { parse } from 'flat-file-test/helpers/parse';
import { module, test } from 'qunit';

module('Unit | Helper | parse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parse([42]);
  assert.ok(result);
});
